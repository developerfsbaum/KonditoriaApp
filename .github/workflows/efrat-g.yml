name: Deploy to EC2

on:
  push:
    branches:
      - efrat-gado

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Connect and deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST_EG }}
          username: ${{ secrets.EC2_USER_EG }}
          key: ${{ secrets.EC2_SSH_KEY_EG }}
          script: |
            cd ~/KonditoriaApp
            git pull origin efrat-gado

            cd react
            npm install
            npm run build
            sudo cp -r dist/* /var/www/html/

            cd ../node.js
            npm install

            sudo systemctl restart bakery.service
