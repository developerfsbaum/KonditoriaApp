name: Deploy to EC2

on:
  push:
    branches:
      - ayala-dermer

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Connect and deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST_AYALA_DERMER }}
          username: ${{ secrets.EC2_USER_AYALA_DERMER }}
          key: ${{ secrets.EC2_SSH_KEY_AYALA_DERMER }}
          script: |
            cd ~/KonditoriaApp
            git pull origin ayala-dermer

            cd react
            npm install
            npm run build
            sudo cp -r dist/* /var/www/html/

            cd ../node.js
            npm install

            sudo systemctl restart bakery.service
